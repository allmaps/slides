{
  "name": "slides-workspace",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "slides dev --config slides.config.yml",
    "build": "slides build --config slides.config.yml",
    "preview": "slides preview --config slides.config.yml",
    "check": "slides check --config slides.config.yml",
    "validate": "slides validate --config slides.config.yml",
    "sync": "slides validate --config slides.config.yml",
    "iiif": "slides iiif --config slides.config.yml"
  },
  "devDependencies": {
    "@allmaps/slides-cli": "workspace:*"
  }
}
